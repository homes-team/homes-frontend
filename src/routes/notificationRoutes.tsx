import { RouteObject } from 'react-router-dom';
import NotificationsPage from '../pages/Notifications';

export const notificationRoutes: RouteObject[] = [{ path: '/notifications', element: <NotificationsPage /> }];
